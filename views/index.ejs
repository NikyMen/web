<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Inicio</title>
</head>
<body>
  <div class="bg-yellow-400 text-blue-900 px-6 py-3 text-center font-semibold rounded mb-6">
  🚚 Envío gratis en compras mayores a $20.000
</div>
<section class="py-10 px-6">
  <h2 class="text-2xl font-bold text-blue-900 mb-6 flex items-center gap-2">
    🔥 Ofertas destacadas
  </h2>

  <div id="carrusel" class="relative w-full overflow-hidden">
    <div id="carruselInner" class="flex transition-transform duration-700 ease-in-out">
      <% productos.forEach((p) => { %>
        <div class="flex-shrink-0 w-full sm:w-1/2 md:w-1/3 lg:w-1/4 p-2">
          <div class="bg-white shadow-md rounded-lg p-4 h-full flex flex-col justify-between">
            <img src="<%= p.imagen %>" alt="<%= p.nombre %>" class="w-full h-40 object-contain mb-3" />
            <h3 class="text-lg font-semibold text-blue-900 mb-1"><%= p.nombre %></h3>
            <p class="text-sm text-gray-600 mb-2"><%= p.descripcion %></p>
            <p class="text-green-600 font-bold text-lg mb-3">$<%= p.precio.toLocaleString("es-AR") %></p>
            <a href="/productos" class="block text-center bg-blue-800 text-white py-2 rounded hover:bg-blue-700 transition">Consultar</a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
</section>

<script>
  const carruselInner = document.getElementById("carruselInner")
  let currentPosition = 0

  setInterval(() => {
    const cardWidth = carruselInner.firstElementChild.offsetWidth + 16
    const totalWidth = carruselInner.scrollWidth
    currentPosition += cardWidth

    if (currentPosition >= totalWidth) {
      currentPosition = 0
      carruselInner.style.transition = "none"
    } else {
      carruselInner.style.transition = "transform 0.7s ease-in-out"
    }

    carruselInner.style.transform = `translateX(-${currentPosition}px)`
  }, 3000)
</script>

</body>
</html>
