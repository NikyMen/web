<section class="py-10 px-6">
  <h2 class="text-2xl font-bold text-blue-900 mb-6 flex items-center gap-2">
    🔥 Ofertas destacadas
  </h2>

  <div class="flex overflow-x-auto gap-4">
    <% productos.forEach((p) => { %>
      <div class="flex-shrink-0 w-64 bg-white shadow-md rounded-lg p-4">
        <img src="<%= p.imagen %>" alt="<%= p.nombre %>" class="w-full h-40 object-contain mb-3" />
        <h3 class="text-lg font-semibold text-blue-900 mb-1"><%= p.nombre %></h3>
        <p class="text-sm text-gray-600 mb-2"><%= p.descripcion %></p>
        <p class="text-green-600 font-bold text-lg mb-3">$<%= p.precio.toLocaleString("es-AR") %></p>
        <a href="/productos" class="block text-center bg-blue-800 text-white py-2 rounded hover:bg-blue-700 transition">Consultar</a>
      </div>
    <% }) %>
  </div>
</section>
